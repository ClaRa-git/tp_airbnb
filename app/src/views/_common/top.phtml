<?php use Symplefony\AbstractSession; ?>

<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Site de locations entre particuliers">

    <!-- Feuilles de styles -->
    <link rel="stylesheet" href="../assets/css/reset.css">
    <link rel="stylesheet" href="../assets/css/style.css">

    <title><?php echo $title ?></title>
</head>

<body>

    <div id="top-container">
        <?php if (AbstractSession::get('user') != null):?>
            <div>
                <div class="logo">
                    <a href="/">AIRBNB</a>
                </div>
                <div>Bonjour <?php echo AbstractSession::get('user')->getfirstName() ?></div>
            </div>
            <div>
                <ul>
                    <?php if(AbstractSession::get('user')->getTypeAccount() == 1): ?>
                    <li><a href="/rentals/user/<?php echo AbstractSession::get('user')->getId() ?>">Mes locations</a></li>
                    <?php endif; ?>
                    <li><a href="/reservations">Mes réservations</a></li>
                </ul>
            </div>         
            <div>
                <a id="logout-button" href="/logout">Se déconnecter</a>
            </div>
        <?php else: ?>
            <div id="top-button">
                <div>
                    <a id="subscribe-button" href="/registration">Créer un compte</a>
                </div>
                <div>
                    <a id="login-button" href="/login">Se connecter</a>
                </div>
            </div>
        <?php endif ?>
    </div>