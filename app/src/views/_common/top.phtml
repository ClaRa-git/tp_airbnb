<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Site de locations entre particuliers">

    <!-- Feuilles de styles -->
    <link rel="stylesheet" href="/assets/css/reset.css">
    <link rel="stylesheet" href="/assets/css/style.css">

    <!-- Police de caractères -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400..700&family=Shantell+Sans:ital,wght@0,300..800;1,300..800&display=swap" rel="stylesheet">

    <title><?php echo $title ?></title>
</head>

<body>
    <div id="top-container">
        <!-- Logo de l'application -->
        <div class="logo">
            <a href="/">PasChezMoi</a>
        </div>

        <?php if ($auth::isAuth()): ?>
            <!-- Informations de l'utilisateur authentifié -->
            <div class="user-info">
                <!-- Affiche le prénom de l'utilisateur -->
                <div class="user-name">Bonjour, <?php echo $auth::getUser()->getfirstName(); ?></div>

                <!-- Affiche le mode de l'utilisateur (Propriétaire ou Utilisateur) -->
                <div class="user-mode">
                    Mode : <?php echo $auth::isOwner() ? 'Propriétaire' : 'Utilisateur'; ?>
                </div>
            </div>

            <!-- Liens spécifiques aux actions d'un utilisateur authentifié -->
            <div class="actions">
                <?php if ($auth::isOwner()): ?>
                    <a href="/rentals/add">Ajouter une location</a>
                <?php endif; ?>
                <a id="reservations-button" href="/reservations/list">Mes réservations</a></li>
                <a id="change-role-button" href="/change-role">Changer de mode</a>
                <a id="signout-button" href="/sign-out">Se déconnecter</a>
            </div>

        <?php else: ?>
            <!-- Liens pour les utilisateurs non authentifiés -->
            <div class="actions">
                <!-- Lien pour créer un compte -->
                <a id="signup-button" href="/sign-up">Créer un compte</a>
                <!-- Lien pour se connecter -->
                <a id="signin-button" href="/sign-in">Se connecter</a>
            </div>
        <?php endif; ?>
    </div>

    <div id="main-container">
        <!-- Contenu de la page -->